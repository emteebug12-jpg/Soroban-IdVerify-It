[package]
name = "idverifier"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "0.28.0", features = ["testutils"] }

[profile.release]
opt-level = "z"
